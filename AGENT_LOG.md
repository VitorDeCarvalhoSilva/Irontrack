# AGENT_LOG.md — Log Cumulativo de Atividades de Agentes (IronTrack)

> Formato e regras completas em [`AGENTS.md`](AGENTS.md) §10. Resumo: este
> log é **append-only** (só adiciona, nunca remove/reescreve linhas
> antigas), uma linha por tarefa/sessão de trabalho, sempre no final da
> tabela. **Leia este arquivo por completo antes de iniciar qualquer
> tarefa no projeto.**

As linhas abaixo, até a marca `-- início do log ao vivo --`, são um
**backfill retroativo** reconstruído a partir do histórico da sessão de
planejamento que originou os documentos `docs/00`-`08` e este próprio
arquivo — registrado de uma vez, de forma resumida, para que o log não
comece "em branco" apesar de todo o trabalho já realizado.

| Data/Hora | Área | Tipo | Resumo | Arquivos Afetados |
| :--- | :--- | :--- | :--- | :--- |
| 2026-07-07 09:00 | Docs | Criação | Handover recebido: `00`-`03` (PRD, Arquitetura, Schema, Contratos API) já existiam de rodada anterior, aprovados. | `00`, `01`, `02`, `03` |
| 2026-07-07 09:30 | Frontend | Criação | `04_FRONTEND_UI_COMPONENTES.md` gerado (rotas, árvore de componentes, UX mobile-first, arquitetura offline-first). | `04` |
| 2026-07-07 10:00 | Frontend | Correção | Corrigidos: numeração de seções (A.x/B.x quebrada), tipos TS não definidos (`WorkoutSet`/`LoadSuggestion`/`RegisterSetPayload`), status `CONFLICT` ausente da union, escopo ambíguo de `/cycles/new`. | `04` |
| 2026-07-07 10:30 | DevOps | Criação | `05_DEVOPS_E_SEGURANCA.md` gerado (Docker, CI/CD, segredos, hardening de segurança). | `05` |
| 2026-07-07 10:45 | DevOps | Correção | Corrigido bug de permissão no Dockerfile do backend (`USER` antes de `COPY`) e ambiguidade no script de backup do SQLite. | `05` |
| 2026-07-07 11:15 | Multi | Correção | Reconciliação cruzada a partir de um planejamento Jira detalhado fornecido pelo usuário: template de dia (`training_day_exercises`), técnicas N:N, `clientGeneratedId`, `refresh_tokens` sem Redis, `load_increment_kg` por exercício, padronização `/api/v1`, fix `{exerciseId}`→`{sessionExerciseId}`, sugestão de carga escopada por sessão, CRUD completo de ciclos/dias/exercícios. | `00`, `02`, `03`, `04` |
| 2026-07-07 12:00 | Backend | Criação | `06_LOGICA_DE_PROGRESSAO.md` gerado (dupla progressão, classificação de séries contáveis, detecção de estagnação, definição de PR) + patch de snapshot imutável em `session_exercises`. | `02`, `03`, `06` |
| 2026-07-07 12:30 | Backend | Correção | Fechados 2 gaps do algoritmo de sugestão (peso heterogêneo entre séries contáveis; séries bônus além de `targetSets`) — CASO 3/CASO 4 redefinidos como mutuamente exclusivos e exaustivos; novo exemplo numérico H.4. | `06` |
| 2026-07-07 13:00 | Backend | Criação | `07_ROADMAP_BACKEND.md` gerado (roadmap sprint a sprint) + patch formalizando ciclo de vida completo de auth, perfil, métricas de dashboard, alertas de estagnação e notificações push. | `02`, `03`, `05`, `07` |
| 2026-07-07 13:30 | Frontend | Criação | `08_ROADMAP_FRONTEND.md` gerado (versão PWA) + patch em `04` (rotas de auth/perfil, dashboard e métricas expandidas, decisão de Recharts, resolução `idb` vs. Dexie.js). | `04`, `08` |
| 2026-07-07 14:00 | Multi | Migração | Migração de stack frontend de PWA (React/Vite) para app nativo (**React Native via Expo**) — reescrita de `01` §3, `04` e `08`; patch em `00` (stack), `02`/`03` (`push_subscriptions` via Expo Push Token em vez de Web Push/VAPID), `05` (remoção de VAPID, deploy Docker/Nginx substituído por EAS Build/Submit/Update), `07` (ajuste pontual da tarefa de push). | `00`, `01`, `02`, `03`, `04`, `05`, `07`, `08` |
| 2026-07-07 14:20 | Docs | Criação | `AGENTS.md` criado — constituição de comportamento para agentes de IA (leitura obrigatória, regra Tipo A/Tipo B, pipeline de execução, permissões/restrições, padrões de código, anti-padrões, checklist final). | `AGENTS.md` |
| 2026-07-07 14:25 | Docs | Auditoria | Inspetor de Projeto disparado em paralelo — auditoria completa de `00`-`08` em busca de resíduos da migração PWA→nativo, referências cruzadas quebradas, inconsistências de nomenclatura, e lacunas de documentação (LGPD, estratégia de testes, glossário, ADRs). Relatório final em `docs/09_RELATORIO_DE_INSPECAO_INICIAL.md` quando concluído. | `00`-`08` (múltiplos), `docs/09_RELATORIO_DE_INSPECAO_INICIAL.md` |

`-- início do log ao vivo --`

| Data/Hora | Área | Tipo | Resumo | Arquivos Afetados |
| :--- | :--- | :--- | :--- | :--- |
| 2026-07-07 14:30 | Docs | Criação | `AGENT_LOG.md` criado — log cumulativo de atividades, com backfill retroativo desta sessão registrado acima da marca de início do log ao vivo. | `AGENT_LOG.md` |
| 2026-07-07 14:50 | Docs | Auditoria | Inspeção geral de `00`-`08` concluída: 14 correções Tipo A aplicadas (destaque: ~27 referências cruzadas fantasmas a `02_SCHEMA_SQLITE.md §3.X`/`§1.X` e `07_ROADMAP_BACKEND.md §1.X` — numeração de prompts de origem já apagados que nunca correspondeu a headings reais; resíduos de terminologia PWA remanescentes em `00`/`01` fora do escopo da rodada de migração; 2 notas de "fora de escopo"/"futuro endpoint" em `06` desatualizadas após alertas de estagnação e PR terem sido implementados em `07`/`03`). 5 achados Tipo B reportados (LGPD/retenção de dados, estratégia de testes, glossário de domínio, ADR log, outras lacunas) com 4 documentos sugeridos (não criados). `prompt.md` desta tarefa arquivado em `docs/_prompts_historico/09_prompt_inspecao.md` (não apagado, por ser tarefa de auditoria — regra `AGENTS.md` §8). | `00`, `01`, `02`, `03`, `04`, `06`, `07`, `docs/09_RELATORIO_DE_INSPECAO_INICIAL.md`, `docs/_prompts_historico/09_prompt_inspecao.md` |
| 2026-07-07 15:15 | Docs | Criação | Revisado o relatório do Inspetor e criados os 5 documentos sugeridos (mais 1 bônus): `10_ESTRATEGIA_DE_TESTES.md` (pirâmide, TDD, ferramentas por camada, Maestro para E2E), `11_POLITICA_DE_PRIVACIDADE_E_RETENCAO_DE_DADOS.md` (LGPD, exclusão em 30 dias) com patch de `deletion_requested_at` em `02` e endpoints `DELETE /users/me`/`POST /auth/cancel-deletion` + `AccountDeletionSchedulerService` em `03`/`07`, `12_GLOSSARIO_DE_DOMINIO.md`, `13_ADR_LOG.md` (11 ADRs retroativos), `14_CATALOGO_DE_ERROS_DE_NEGOCIO.md` (13 códigos de erro consolidados). Adicionado mandato de TDD obrigatório (Seção 6.1) ao `AGENTS.md`. `README.md` reescrito (estava com encoding corrompido e desatualizado citando "React" puro) com pitch do produto, stack, tabela de todos os docs `00`-`14`, seção de governança de agentes, e placeholder de setup local. | `02`, `03`, `07`, `10`, `11`, `12`, `13`, `14`, `AGENTS.md`, `README.md` |
| 2026-07-07 15:45 | Multi | Auditoria | Auditoria de completude de `07`/`08` (cobertura endpoint-a-endpoint e tela-a-tela): backend 100% coberto, mas 4 gaps reais no par `04`/`08` — `SessionHistoryScreen` inexistente (requisito de `00` §4.5/EP-03 sem UI planejada), `DashboardScreen` sem tarefa de criação explícita (Sprint 3/`ActiveWorkoutScreen` seria inatingível), `GET /metrics/overview` sem tarefa de UI, e UI de exclusão de conta ausente do `ProfileScreen` (criada depois de `11` já existir). Mais 1 gap Tipo B (algoritmo de "dia sugerido" nunca definido) resolvido com o usuário optando pela abordagem mais simples (rotação por `orderIndex`, sem dia da semana). Todos os 6 corrigidos: `nextSuggestedTrainingDayId` (patch em `03` §3.2 + tarefa em `07` Sprint 2), `SessionHistoryScreen` (rota em `04` + tarefa em `08` Sprint 3), `DashboardScreen` faseada (criação em `08` Sprint 2, botão "Iniciar Treino" ligado na Sprint 3), `Dashboard/OverviewCards` (`08` Sprint 4), botão "Excluir conta" (`04` `ProfileScreen` + `08` Sprint 1), tarefa explícita de Swagger/OpenAPI (`07` Sprint 0). | `03`, `04`, `07`, `08` |
| 2026-07-08 09:00 | Docs | Auditoria | Segunda auditoria completa de `00`-`14`/`AGENTS.md` sob a ótica de "prontidão para implementação autônoma por agente de IA" (o objetivo de `09` foi consistência interna; o desta rodada foi a executabilidade). Veredito: documentação já em nível incomumente alto de precisão executável (contratos com regra de negócio embutida, algoritmo formalizado com casos de teste literais, roadmaps apontando classe/endpoint exatos) — decisão explícita do usuário de **não** criar novos documentos hiperdetalhados, pois aumentaria superfície de manutenção sem benefício proporcional. 1 achado Tipo B crítico + 3 achados Tipo A corrigidos nesta sessão (ver linha abaixo). | — |
| 2026-07-08 09:30 | Docs | Correção | Corrigida contradição de escopo Tipo B (decisão do usuário: reverter): `01` §7 ("Flexibilidade de Domínio — Requisito Obrigatório") exigia motor de progressão polimórfico (Strategy/Factory) para calistenia isométrica/habilidade (SKILL), contradizendo `00`, `AGENTS.md` §2, `03` §1.1 e o próprio `06` (spec final, só cobre `STRENGTH`). Reescrita para manter apenas a tolerância de schema como extensibilidade futura, sem requisito de implementação — registrado como ADR-012. Fechadas 4 decisões técnicas antes em aberto (Java 21/Spring Boot 3.3.x/jjwt/Flyway obrigatório no backend — `01` §2.4, ADR-013; sem Lombok — ADR-014; `react-hook-form`+`zod` no frontend — `01` §3.4, ADR-015) para eliminar escolhas implícitas de biblioteca na hora de codificar. Removida ambiguidade "Context API ou Zustand" (`01` §3.3, `AGENTS.md` §2) — Zustand nunca é usado em nenhuma tarefa de `08`, decisão fechada para Context API puro. Adicionado `TOO_MANY_LOGIN_ATTEMPTS`/429 ao `14` (condição já existia em `05` §E.2 mas faltava no catálogo consolidado). | `01`, `AGENTS.md`, `07`, `13`, `14` |
| 2026-07-08 10:15 | Multi | Correção | Usuário notou confusão de escopo ao orquestrar outro agente (viu calistenia sobrando em `01`/`02` mesmo após ADR-012). Explicado o histórico completo e apresentada a decisão: usuário escolheu remover **totalmente** a tolerância de schema (indo além de ADR-012), não apenas generalizar. `02_SCHEMA_SQLITE.md` reescrito removendo fisicamente `progression_steps`, colunas `hold_time_seconds`/`hold_time_target`/`progression_step_id` de `exercise_sets`, e a coluna `exercise_library.type` inteira (só tinha um valor real possível); seed simplificado para conter apenas exercícios de musculação; e-mail de exemplo trocado de `gabriel.calistenia@` para `gabriel.silva@`. `03_CONTRATOS_API.md` idem (removidos `type`/`holdTimeSeconds`/`holdTimeTarget`/`progressionStepId` dos exemplos JSON, e-mail trocado). `01` §7 reescrita pela 2ª vez (agora sem nenhuma tolerância, só orientação de que uma expansão futura exige decisão Tipo B nova). `07` Sprint 0 ajustada (seed sem menção a calistenia). Registrado como `ADR-016` (supersede `ADR-012`) em `13`. Também aproveitado para corrigir 2 resíduos textuais menores em `01` (exemplo de coluna `hold_time_seconds` na tabela de nomenclatura) e `AGENTS.md` §2 (ainda descrevia o schema como "extensível" para calistenia). | `01`, `02`, `03`, `07`, `13`, `AGENTS.md` |
| 2026-07-08 11:00 | Multi | Auditoria | Varredura final de escopo executada a partir de `prompt.md` (usuário pediu execução + exclusão do arquivo ao final — conflito sinalizado: o próprio `prompt.md` pedia arquivamento em `docs/_prompts_historico/` por ser tarefa de auditoria, `AGENTS.md` §8; a instrução explícita do usuário nesta conversa prevalece, `AGENTS.md` preâmbulo, então o arquivo foi apagado, não arquivado). **Parte A (resíduo de calistenia/isometria):** buscados os termos `STRENGTH`/`ISOMETRIC`/`SKILL`/`progression_step`/`hold_time`/`Handstand`/`Planche`/`Muscle-Up`/`calisten*`/`isometr*`/`habilidade` em todo o repositório. 2 resíduos reais encontrados fora das exceções (`13_ADR_LOG.md` ADR-012/016 e `AGENT_LOG.md`, protegidos por serem histórico append-only): (1) o quadro "Histórico" de `01` §7 ainda narrava em prosa o que fora removido — reescrito para só apontar para as ADRs, sem repetir o vocabulário; (2) `03` §1.1 ainda dizia que dados de isometria/habilidade estavam "desabilitados/omitidos desta camada de transporte", frase agora factualmente incorreta (não existem em nenhuma camada) — corrigida para referenciar `ADR-016` diretamente. Um 3º resíduo equivalente encontrado em `AGENTS.md` §2 (mesmo documento de leitura sempre obrigatória) e corrigido da mesma forma. **Parte B (completude de musculação):** conferido, camada a camada (schema↔contrato↔tela↔roadmap), que Exercício (nome/grupo muscular/`load_increment_kg`/custom), Treino (ciclo→dia→template), Série (peso/reps/RPE/técnicas/`clientGeneratedId`) e Sessão (início/registro/finalização/histórico, incl. `SessionHistoryScreen`) permanecem completos e consistentes entre `02`/`03`/`04`/`06`/`07`/`08` após a remoção de `ADR-016` — nenhuma lacuna encontrada. Nenhum achado Tipo B. | `01`, `03`, `AGENTS.md` |
| 2026-07-08 11:20 | Multi | Auditoria | Segunda verificação independente da varredura final (a pedido do usuário, papel de "engenheiro conferindo antes do início do desenvolvimento"). Confirmado via grep total do repositório: nenhum resíduo de calistenia/isometria fora de `13_ADR_LOG.md`/`AGENT_LOG.md` (histórico protegido); nenhum "TBD"/"a definir"/"FIXME" real (só negações do tipo "nenhum X é deixado como Y"); numeração de seções de `01` (§1-§7) e `03` (§1.1-§7.4) 100% sequencial, sem lacunas/duplicatas; `Dockerfile` do backend (`05`) consistente com Java 21 (`ADR-013`); `Flyway` referenciado como obrigatório em `07` §C.0 (não mais "recomendado"). **1 gap novo encontrado e corrigido:** `react-hook-form`+`zod` (`01` §3.4, `ADR-015`) nunca era mencionado em `08_ROADMAP_FRONTEND.md` — nenhuma tarefa de tela com formulário (Login/Register/ForgotPassword/ResetPassword/Profile/CycleForm/criação de exercício) instruía o agente a usá-lo, deixando a porta aberta para `useState` manual. Adicionada a instalação no Sprint 0 (`08` §C.0, item 9) e a referência explícita em cada tarefa de tela com formulário nas Sprints 1 e 2. Veredito comunicado ao usuário: documentação pronta para início de desenvolvimento. | `08`, `AGENT_LOG.md` |
| 2026-07-08 12:00 | Backend | Criação | Sprint 0 (Fundação Técnica) implementada integralmente a partir de `prompt-backend-sprint0.md` — as 7 tarefas exatas de `07_ROADMAP_BACKEND.md` §C.0: (1) projeto Spring Boot criado em `backend/` (pacote `com.irontrack.api`, árvore de `01` §2.1, Java 21 + Spring Boot 3.3.13 + jjwt 0.12.7 + Flyway + `spring-boot-starter-validation` + springdoc, sem Lombok); (2) datasource SQLite via `DATABASE_PATH` (env), `PRAGMA foreign_keys=ON` via parâmetro de URL do driver `sqlite-jdbc`, `application-dev.properties` e `backend/.env.example` criados, `.gitignore` da raiz atualizado (`.env`, `target/`, `*.db`); (3) 12 migrations Flyway (`V1`-`V12`) copiando literalmente o DDL de `02_SCHEMA_SQLITE.md` §2 (users→refresh_tokens→training_cycles→training_days→training_day_exercises→exercise_library→training_sessions→session_exercises→exercise_sets→exercise_set_techniques→stagnation_alerts→push_subscriptions, incluindo todos os índices de §4); (4) seed expandido para 56 exercícios (peito/costas/ombros/bíceps/tríceps/pernas/glúteos/core, `V13__seed_exercises.sql`) isolado em `db/dev-migration/` e ativado só pelo perfil `dev` (nunca produção); (5) `GlobalExceptionHandler` (`exceptions/`) com payload exato de `03` §1.4 + campo opcional `errorCode` (omitido via `@JsonInclude` quando nulo, pronto para `14_CATALOGO_DE_ERROS_DE_NEGOCIO.md`) e mapeamento 400/401/403/404/422/500; (6) `SecurityConfig` + `JwtAuthenticationFilter` placeholder (`security/`) com sessão stateless, endpoints `/api/v1/auth/**` e Swagger públicos, e os 4 cabeçalhos de `05` §E.1 (`X-Content-Type-Options`, HSTS, `X-Frame-Options`, CSP `default-src 'none'`); (7) `OpenApiConfig` (springdoc) com `/swagger-ui.html` funcional. **1 correção Tipo A a registrar:** `01` §2.4 citava `org.flywaydb:flyway-database-sqlite` como módulo obrigatório — artefato inexistente no Maven Central (confirmado via busca), pois o suporte a SQLite já vem embutido em `flyway-core` desde antes da versão gerenciada por Spring Boot 3.3.x (10.10.0); usado apenas `flyway-core`, sem módulo extra (decisão de ferramenta/ADR-013 inalterada, só a coordenada de artefato estava desatualizada). `springdoc-openapi-starter-webmvc-ui` fixado em `2.6.0` (não a última versão do Maven Central) por ser a última linha compatível com Spring Framework 6.1.x/Spring Boot 3.3.x — versões 2.7+ exigem Spring Boot 3.4+. JaCoCo configurado no `pom.xml` (gate real de 80% só passa a valer a partir da Sprint 1, quando houver `services/` de regra de negócio). Verificação final completa: `mvn clean install` compila e roda 8 testes (6 unitários + `FlywayMigrationIT` via `maven-failsafe-plugin`) com sucesso; app testado manualmente com `mvn spring-boot:run -Dspring-boot.run.profiles=dev` — todas as 13 migrations aplicadas, `/swagger-ui.html` e `/api-docs` respondem 200, cabeçalhos de segurança confirmados via `curl -I`, nenhum segredo real commitado. `prompt-backend-sprint0.md` apagado conforme instrução do próprio arquivo. | `backend/` (novo), `.gitignore` |
| 2026-07-08 13:00 | Frontend | Criação | Sprint 0 (Fundação Técnica) implementada integralmente a partir de `prompt-frontend-sprint0.md` — as 9 tarefas exatas de `08_ROADMAP_FRONTEND.md` §C.0: (1) projeto Expo TypeScript em `frontend/` (React 19.2.3, React Native 0.86.0, Expo SDK 57 — já criado em sessão anterior, validado nesta); (2) `eas.json` com os 3 perfis (`development`/`preview`/`production`, texto idêntico ao de `05` §A.2) e `app.json` atualizado (nome "IronTrack", slug "irontrack", `bundleIdentifier`/`package` `com.irontrack.app`, splash via plugin `expo-splash-screen` instalado nesta sessão); (3) ESLint (`eslint-config-expo` + `eslint-config-prettier`/`eslint-plugin-prettier` encadeados) e Prettier (`.prettierrc.json`/`.prettierignore`) configurados, com scripts `lint`/`format`/`format:check`/`typecheck`/`test` adicionados ao `package.json` (os mesmos nomes já assumidos pelo CI de `05` §B); (4) árvore de pastas completa de `01` §3.1/`08` §B criada na raiz de `frontend/` (`components/{common,layout}`, `screens/{Auth,Dashboard,WorkoutCycle,ActiveWorkout}`, `navigation/`, `hooks/`, `contexts/`, `services/storage/`, `utils/`, `theme/`, `types/`), pastas ainda vazias desta sprint marcadas com `.gitkeep`; (5) `services/apiClient.ts` — Axios com `baseURL` de `EXPO_PUBLIC_API_BASE_URL` e interceptor de resposta que normaliza o payload de erro de `03` §1.4 (`message`) em um `Error` JS, sem lógica de refresh (Sprint 1); `.env.example` criado, `.env` real adicionado ao `.gitignore` (gap encontrado: só `.env*.local` estava coberto); (6) 4 componentes atômicos em `components/common/` (`Button`, `Input`, `Card`, `Modal`) via NativeWind, Touch Targets 44x44/56x56 (`04` §C.2) e `Input` repassando `keyboardType` livremente para os campos numéricos que as telas de sessão vão usar (`04` §C.1), cada um com teste de render/interação (`@testing-library/react-native`); (7) NativeWind configurado (`tailwind.config.js`, `babel.config.js` com preset `nativewind/babel`, `metro.config.js` com `withNativeWind`, `global.css`, `nativewind-env.d.ts`); (8) `navigation/RootNavigator.tsx` alternando `AuthStack`/`AppStack` (cada um com uma única tela placeholder) observando `contexts/AuthContext.tsx` (placeholder fixo `isAuthenticated: false`), sem nenhuma tela de negócio; (9) `react-hook-form`+`zod`+`@hookform/resolvers/zod` já instalados (sessão anterior), confirmados presentes. **2 gaps Tipo A corrigidos nesta sessão:** `.gitignore` do frontend só ignorava `.env*.local`, não `.env` puro (violação direta de `01` §6.1) — corrigido; `@testing-library/react-native` instalado é a v14.0.1, cuja API `render`/`fireEvent.press` tornou-se assíncrona (breaking change desde as versões documentadas historicamente) — todos os testes ajustados para `await`. Verificação final completa: `npx tsc --noEmit`, `npx eslint .` e `npx jest --coverage` (9 testes, 4 suítes) passam sem erro; `npx expo-doctor` 20/20; `npx expo start` sobe o Metro Bundler e serve o bundle Android completo (1369 módulos) sem erro; `npx eas-cli build --profile development` avança até o gate de login da conta Expo (`EXPO_TOKEN`/`eas login` ausentes neste ambiente sandbox, sem credenciais) — falha é de autenticação, não de configuração do `eas.json`/`app.json`, confirmando que a esteira EAS está corretamente configurada. `prompt-frontend-sprint0.md` apagado conforme instrução do próprio arquivo. | `frontend/` (múltiplos), `AGENT_LOG.md` |
| 2026-07-08 13:20 | Docs | Correção | Usuário reportou que o Expo não roda no ambiente dele — causa raiz: `prompt-frontend-sprint0.md` mandava `npx create-expo-app@latest` sem fixar SDK, e resolveu para SDK 57 (React 19.2.3/RN 0.86.0, ver entrada anterior), 3 versões à frente do que o usuário precisa. Travado **Expo SDK 54** como decisão definitiva: nova `01_ARQUITETURA_E_PADROES.md` §3.5 (espelhando o §2.4 do backend — sempre via `npx expo install --fix`, nunca fixar `react-native`/`react` manualmente), registrado como `ADR-017` em `13`. Corrigida a tarefa 1 de `08_ROADMAP_FRONTEND.md` §C.0 para referenciar a versão travada. Criado `prompt-fix-expo-sdk54.md` para o agente de frontend: como downgrade de 3 versões de SDK via `expo install --fix` não é confiável, a instrução é recriar `frontend/` do zero na 54 e reaplicar fielmente a checklist já implementada (extraída da entrada de log anterior, incluindo os 2 gaps já corrigidos naquela sessão — `.gitignore` de `.env` e API assíncrona do `@testing-library/react-native` v14). | `01`, `08`, `13`, `AGENT_LOG.md` |
| 2026-07-08 14:30 | Frontend | Correção | Executado `prompt-fix-expo-sdk54.md`: causa raiz real do erro do usuário confirmada via API pública do Expo Go (`exp.host/--/api/v2/versions`) — o cliente Expo Go distribuído nas lojas hoje só cobre até o SDK 54 (`expoGoSdkVersion`); SDK 57 exige sideload manual ou dev build, o que explicava a falha mesmo com `expo-doctor` limpo. `frontend/` (untracked, sem rede de segurança do git) renomeado para backup antes de qualquer ação destrutiva; 2 processos `expo start` órfãos de sessão anterior travando o diretório (`Device or resource busy` no `mv`) encerrados antes do rename. Projeto recriado via `create-expo-app` + `expo install expo@54.0.0` + `expo install --fix`, com `react-native-reanimated`/`react-native-worklets` adicionados explicitamente (peer dependency do NativeWind via `react-native-css-interop` que não estava fixada em nenhuma versão antes, causando módulos nativos duplicados detectados pelo `expo-doctor`) — reinstalação limpa (`node_modules`/lockfile apagados e reinstalados do zero) necessária para que `react-native-css-interop` fosse hoisted corretamente na raiz (senão o Jest falhava com `Cannot find module 'react-native-css-interop/jsx-runtime'`). Reaplicada fielmente toda a árvore já validada (`components/common`, `navigation`, `contexts/AuthContext`, `services/apiClient.ts`, `types`, pastas vazias com `.gitkeep`, NativeWind, ESLint/Prettier, `eas.json`/`app.json`, `.gitignore` já com o fix de `.env` puro). `AGENTS.md` do template (`Read the exact versioned docs at .../v57.0.0/`) corrigido para `v54.0.0`. Verificação final completa: `tsc --noEmit`, `eslint .` e `jest --coverage` (9 testes, 4 suítes) sem erro; `expo-doctor` 18/18; `expo start` bundlou 1288 módulos Android com sucesso (HTTP 200 no bundle). Também removidos como lixo órfão (não documentado em nenhum `01`-`14`, untracked, causa da 2ª instância de `expo start` travando o diretório): `package.json`/`package-lock.json`/`tsconfig.json`/`node_modules`/`.expo` soltos na raiz do repositório (continham só `expo@^57.0.4` avulso, sem propósito). `prompt-fix-expo-sdk54.md` apagado conforme instrução do próprio arquivo. | `frontend/` (recriado), `AGENT_LOG.md` |
| 2026-07-08 15:10 | Multi | Auditoria | Inspeção direta do código gerado nas Sprints 0 (backend + frontend), a pedido do usuário ("verifique... e me fale se está tudo ok para a Sprint 1"), não apenas releitura do log — arquivos abertos e lidos de fato. **Backend:** `pom.xml`, 12 migrations Flyway, `GlobalExceptionHandler`, `SecurityConfig`, `application.properties` conferidos linha a linha contra `01`/`02`/`03`/`05`/`07` — tudo consistente (Java 21/Spring Boot 3.3.13/jjwt 0.12.7 corretos; payload de erro e mapeamento de status batendo com `03` §1.4; os 4 cabeçalhos de segurança de `05` §E.1 presentes; `PRAGMA foreign_keys=on` via parâmetro de URL do driver, correto). **Frontend:** `package.json` (Expo `~54.0.35`/RN `0.81.5` — par correto, resolvido via `expo install --fix`), `App.tsx`, `AuthContext.tsx`, `RootNavigator.tsx`, `apiClient.ts`, `Input.tsx` conferidos — todos fiéis ao escopo de placeholder da Sprint 0, sem nenhuma tela de negócio adiantada. **2 achados Tipo A corrigidos:** (1) `01_ARQUITETURA_E_PADROES.md` §2.4 e `13_ADR_LOG.md` ADR-013 ainda citavam `org.flywaydb:flyway-database-sqlite` como módulo obrigatório — artefato que o agente de backend já tinha descoberto não existir no Maven Central (usou só `flyway-core`, corretamente), mas a documentação nunca foi atualizada para refletir isso; corrigida em ambos os documentos. (2) Pasta `.expo/` órfã na raiz do repositório (resíduo de um comando `expo` rodado no diretório errado em sessão anterior, não dentro de `frontend/`) — removida, e adicionada regra `/.expo/` ao `.gitignore` da raiz para prevenir recorrência. **Achados não-bloqueantes, apenas observados:** `frontend/AGENTS.md`/`CLAUDE.md`/`LICENSE` são boilerplate do template `create-expo-app` (não gerados por nenhuma tarefa deste projeto) — o `AGENTS.md` do template é só um lembrete de 2 linhas sobre a versão do SDK (já corrigido para v54 pelo agente de frontend), sem conflito de conteúdo com o `AGENTS.md` da raiz, mas com risco de confusão de nome; `LICENSE` implica MIT sem nenhuma decisão de licenciamento real ter sido tomada (README ainda diz "a definir") — nenhum dos dois foi alterado nesta rodada, ambos comunicados ao usuário para decisão. Veredito comunicado ao usuário: Sprint 0 de ambos aprovada, pronto para Sprint 1. | `01`, `13`, `.gitignore`, `AGENT_LOG.md` |
| 2026-07-08 15:30 | Docs | Correção | Antes de gerar os prompts da Sprint 1, encontrada lacuna real em `03_CONTRATOS_API.md` §2.11 (`POST /auth/cancel-deletion`): descrevia um mecanismo de "link por e-mail" mas nunca definia request body nem o passo de confirmação — teria bloqueado a implementação de `UserService.cancelDeletion` na Sprint 1. Simplificado (Tipo A/editorial, não é uma nova decisão de produto): o endpoint agora reverifica `email`+`password` diretamente (mesmo padrão de `DELETE /users/me`, §2.10), sem token/link por e-mail — justificativa: a conta já fica bloqueada de login enquanto a exclusão está pendente, então a senha sozinha já é prova de posse suficiente. Atualizados em conjunto: `11_POLITICA_DE_PRIVACIDADE_E_RETENCAO_DE_DADOS.md` (passo 3 da Seção B e Seção D) e `14_CATALOGO_DE_ERROS_DE_NEGOCIO.md` (`INVALID_PASSWORD` agora cobre os dois endpoints; `NO_PENDING_DELETION` reformulado sem menção a expiração de token). | `03`, `11`, `14` |
| 2026-07-08 15:35 | Docs | Correção | Propagada a mudança de `03` §2.11 (fim do link por e-mail) para `04_FRONTEND_UI_COMPONENTES.md` (descrição de `ProfileScreen`/`LoginScreen`) e `08_ROADMAP_FRONTEND.md` §C.1 (task 3) — o cancelamento de exclusão agora é um botão inline em `LoginScreen` ao receber `ACCOUNT_PENDING_DELETION`, reenviando `email`/`password` já digitados, não mais uma tela reaproveitada de link por e-mail. | `04`, `08` |
| 2026-07-08 16:00 | Frontend | Criação | Executado `prompt-frontend-sprint1.md` (Autenticação e Perfil, `08_ROADMAP_FRONTEND.md` §C.1) — as 7 tarefas na ordem: (1) `AuthContext`/`useAuth` reais via `useReducer`, reducer puro (`contexts/authReducer.ts`) desenvolvido em TDD (`authReducer.test.ts` escrito e vermelho antes da implementação, `AGENTS.md` §6.1); (2) `LoginScreen`/`RegisterScreen`/`VerifyEmailScreen`/`ForgotPasswordScreen`/`ResetPasswordScreen` em `screens/Auth/`, todas com `react-hook-form`+`zod` (schema por tela, `passwordRule.ts` compartilhado); (3) botão inline "Cancelar exclusão da conta" em `LoginScreen` ao receber `errorCode: ACCOUNT_PENDING_DELETION`; (4) `ProfileScreen` com edição de perfil, troca de senha, botão "Excluir conta" (diálogo via `Modal` pedindo senha) e bloco estático de preferências de push (sem chamada de rede, Sprint 5); (5) interceptor de refresh automático em `apiClient.ts` (única tentativa por requisição via flag `_retry`, single-flight de refresh concorrente) — lógica extraída para `handleResponseError` exportado e testado em TDD (`apiClient.test.ts`, 6 casos: mapeamento de erro comum, endpoints de auth nunca disparam refresh, refresh com sucesso reconcilia token e refaz a chamada, refresh sem `refreshToken` local, refresh que falha, e proteção contra loop); (6) `RootNavigator`/`AuthStack`/`AppStack` conectados ao `AuthContext` real; (7) `expo-secure-store` instalado e usado em `services/tokenStorage.ts` para o `refreshToken` (`accessToken` só em memória, `services/apiClient.ts`). `types/api.types.ts` expandido com todos os DTOs de `03_CONTRATOS_API.md` §2 e o union `ErrorCode` espelhando `14_CATALOGO_DE_ERROS_DE_NEGOCIO.md`. **2 reconciliações Tipo A não previstas literalmente pelo prompt, aplicadas e registradas:** (a) `VerifyEmailScreen`/`ResetPasswordScreen` foram registradas no navigator **raiz** (`navigation/types.ts` `RootStackParamList`), não dentro de `AuthStack` como a tarefa 6 sugeria ao pé da letra — a leitura obrigatória da própria tarefa (item 6 da Seção 0) já apontava para `04_FRONTEND_UI_COMPONENTES.md` §A.1, que exige essas duas telas alcançáveis via deep link **mesmo com sessão autenticada**, algo impossível se vivessem só dentro de `AuthStack` (que só é montado quando não-autenticado); (b) troca de senha e exclusão de conta (`03_CONTRATOS_API.md` §2.9/§2.10) revogam **todos** os `refresh_tokens` do usuário no backend, incluindo o da sessão atual — `ProfileScreen` agora chama `logout()` local logo após essas duas operações terem sucesso, em vez de deixar a UI num estado autenticado que o próximo request derrubaria de forma confusa. **1 dependência nova fora da lista textual de `AGENTS.md` §2 (Stack Travada):** `expo-linking`, necessária para o deep link de verificação de e-mail/reset de senha — não é uma introdução silenciosa: o próprio `prompt-frontend-sprint1.md` (tarefa 2) já nomeava `expo-linking` explicitamente como parte da tarefa; `AGENTS.md` §2 deveria ser atualizado para incluí-la (não feito nesta sessão, sinalizado aqui). Esquema de deep link definido como `irontrack://` (`app.json` `scheme`), com paths `verify-email/:token`/`reset-password/:token` — combinação com o time de backend ainda pendente (nenhum agente de backend rodou em paralelo nesta sessão). `ProfileScreen` foi colocada em `screens/Auth/` por não existir uma pasta própria de "Profile" em `01_ARQUITETURA_E_PADROES.md` §3.1 (só `Auth`/`Dashboard`/`WorkoutCycle`/`ActiveWorkout`) — mesma sprint/domínio de `03_CONTRATOS_API.md` §2, decisão de local de arquivo, não arquitetural. Verificação final completa: `tsc --noEmit`, `eslint .` (0 erros, aplicado `--fix` para os warnings de formatação) e `jest --coverage` (22 testes, 6 suítes, todos verdes) sem erro; `expo-doctor` 18/18 (1 duplicata de `expo-constants` aninhada sob `expo-linking` corrigida via reinstalação limpa + `npm dedupe`); `npx expo start` bundlou 1478 módulos Android com sucesso. **Não feito nesta sessão, sinalizado explicitamente:** teste manual do fluxo completo contra um backend real (nenhum backend rodando/disponível nesta sessão) — apenas verificado que o app compila, tipa, linta, testa e bundla; recomenda-se rodar o fluxo registro→verificação→login→perfil→logout manualmente assim que `prompt-backend-sprint1.md` estiver implementado. `prompt-frontend-sprint1.md` apagado conforme instrução do próprio arquivo. | `frontend/` (contexts, hooks, navigation, screens/Auth, services, types), `app.json`, `AGENT_LOG.md` |
| 2026-07-08 18:45 | Backend | Criação | Executado `prompt-backend-sprint1.md` (Autenticação e Perfil, `07_ROADMAP_BACKEND.md` §C.1) — as 7 tarefas na ordem, todas em TDD (`AGENTS.md` §6.1, teste escrito antes da implementação): (1) entidades `User`/`RefreshToken` (mapeamento exato de `02`, getters/setters explícitos, sem Lombok) + `InstantIso8601Converter` (Instant↔TEXT ISO8601, TDD); (2) DTOs `record` para todo endpoint de `03` §2 + `StrongPassword`/`StrongPasswordValidator` (Bean Validation customizado, TDD, compartilhado entre Register/Reset/ChangePassword); (3) `AuthService`/`AuthController` (register/login/refresh/logout/verifyEmail/forgotPassword/resetPassword) + `LoginAttemptService` (Caffeine, 5 falhas/15min, testado isoladamente primeiro conforme o prompt pedia) + `TokenHasher` (SHA-256, TDD); (4) `UserService`/`UserController` (me/updateProfile/changePassword/requestDeletion/cancelDeletion) + `PushSubscriptionCleanupService` (nova interface, isola o único `DELETE` via SQL bruto para manter `UserService` testável por mock); (5) `AccountDeletionSchedulerService` (`@Scheduled(cron = "0 0 3 * * *")`, `@EnableScheduling` adicionado à classe principal); (6) `JwtTokenProvider` (jjwt, claim `jti` único por token, TDD); (7) `EmailService`/`SmtpEmailService` (loga em vez de enviar quando `SMTP_HOST` vazio, deep link `irontrack://verify-email/{token}` e `irontrack://reset-password/{token}` — mesmo esquema já adotado pelo frontend na sessão anterior, sem necessidade de combinação adicional) + teste de integração real via GreenMail embarcado. Pré-requisito da Seção 1 do prompt resolvido antes da tarefa principal: `BadRequestException`/`TooManyRequestsException` criadas e mapeadas para 400/429 no `GlobalExceptionHandler`; `UnauthorizedException`/`ForbiddenException` estendidas com `errorCode` opcional (mesmo padrão já existente em `BusinessRuleException`). Todos os 9 `errorCode`s do mapa da Seção 2 do prompt implementados e verificados manualmente via `curl` fim a fim (register→verify→login→refresh→change-password→delete→cancel-deletion→forgot/reset-password→rate limit). **1 gap real de documentação encontrado e fechado (Tipo A, convenção já prevista pelo próprio `14`):** `POST /auth/register` não tinha nenhum `errorCode` definido para e-mail duplicado — adicionado `EMAIL_ALREADY_REGISTERED` (422) em `14_CATALOGO_DE_ERROS_DE_NEGOCIO.md` e `03_CONTRATOS_API.md` §2.1. **4 bugs reais encontrados e corrigidos durante a verificação manual (não apenas nos testes automatizados, que não os pegavam):** (a) dois tokens JWT emitidos para o mesmo usuário no mesmo segundo eram byte-idênticos (`iat`/`exp` truncam para segundos) — `JwtTokenProvider` passou a incluir claim `jti` (UUID) único por token; (b) `application-dev.properties` (Sprint 0) definia `SMTP_HOST=localhost`, incompatível com a regra "loga em vez de envia quando vazio" — `POST /auth/register` retornava `500` em qualquer ambiente dev sem um SMTP de teste rodando; corrigido para `SMTP_HOST=` vazio por padrão; (c) `GlobalExceptionHandler.handleGeneric` não logava a exceção capturada — um `500` real não deixava nenhum rastro no servidor para diagnóstico; adicionado `log.error(..., ex)`; (d) requisição sem token a um endpoint autenticado respondia `403` **sem corpo** via `Http403ForbiddenEntryPoint` padrão do Spring Security, nunca passando pelo `GlobalExceptionHandler` — violava tanto o mapeamento de status de `01` §4.1 (token ausente é `401`, não `403`) quanto o payload padronizado de `03` §1.4 para o caso de erro mais comum da API; corrigidos com `RestAuthenticationEntryPoint`/`RestAccessDeniedHandler`/`SecurityExceptionResponseWriter` novos (TDD), conectados via `.exceptionHandling()` no `SecurityConfig`. Também substituído o placeholder do `JwtAuthenticationFilter` da Sprint 0 pela validação real (extrai `Authorization: Bearer`, valida via `JwtTokenProvider`, popula `SecurityContext` com o `userId`) — pré-requisito implícito da tarefa 4 (nenhum endpoint de perfil funciona sem isso), TDD. Verificação final completa: `mvn clean install` — 83 testes unitários + 9 de integração (`FlywayMigrationIT`, `SmtpEmailServiceIT`/GreenMail, `JdbcPushSubscriptionCleanupServiceIT`, `AuthControllerIT`, `UserControllerIT` via MockMvc contra SQLite real) verdes; gate real de cobertura JaCoCo ativado em `0.80` (`pom.xml`, era `0.00` desde a Sprint 0) e passa (~90% de linha no bundle, ~100% nas classes de `services`/`security`). `prompt-backend-sprint1.md` apagado conforme instrução do próprio arquivo. | `backend/` (múltiplos), `docs/03_CONTRATOS_API.md`, `docs/14_CATALOGO_DE_ERROS_DE_NEGOCIO.md`, `AGENT_LOG.md` |
| 2026-07-08 19:00 | Multi | Correção | Lidas as entradas completas de Sprint 1 (backend 18:45, frontend 16:00) antes de planejar o próximo passo pedido pelo usuário (testagem completa do backend + integração + 2 diretrizes de UX de redirecionamento). Achado e corrigido: `04_FRONTEND_UI_COMPONENTES.md` nunca especificava o redirecionamento de `RegisterScreen` pós-sucesso (só `LoginScreen`→`AppStack` e `ResetPasswordScreen`→`LoginScreen` já estavam documentados) — adicionada a especificação exata (navega para `LoginScreen` com mensagem de confirmação, nunca autentica automaticamente, já que o backend exige `email_verified_at`). Confirmado que `expo-linking` já tinha sido adicionado à Seção 2 (Stack Travada) do `AGENTS.md` (pendência sinalizada pelo agente de frontend na Sprint 1, já resolvida por outra sessão — nenhuma ação necessária). Criados `prompt-backend-hardening.md` (auditoria de robustez independente — concorrência, tokens adulterados/reutilizados, fuzzing leve de validação, verificação de que o padrão do bug (d) da Sprint 1 não se repete em outra rota, higiene de config — deliberadamente **não** repete o que a Sprint 1 já testou) e `prompt-frontend-ux-fix.md` (fecha a lacuna de `RegisterScreen`, confirma `LoginScreen`, ambos com teste). | `04`, `AGENT_LOG.md` |
| 2026-07-09 09:30 | Frontend | Correção | Executado `prompt-frontend-ux-fix.md`. (1) `RegisterScreen`: removida a view inline "verifique seu e-mail" que existia desde a Sprint 1 — agora, após `POST /auth/register` (201), navega para `Login` (`AuthStackParamList.Login` ganhou o parâmetro opcional `confirmationMessage`) com "Conta criada! Verifique seu e-mail e faça login.", nunca autentica automaticamente (`04_FRONTEND_UI_COMPONENTES.md` §A, especificação adicionada na entrada anterior). `LoginScreen` passou a ler `route.params?.confirmationMessage` e exibir um banner verde acima do formulário. (2) `LoginScreen`: confirmado por leitura de código (não só do log) que o sucesso de `POST /auth/login` já não navega manualmente em nenhum ponto — `login()` só atualiza `AuthContext`, e o `RootNavigator` troca `AuthStack`→`AppStack` sozinho observando `isAuthenticated`; nada precisou ser corrigido aqui, só testado. (3) Testes de componente criados (`RegisterScreen.test.tsx`, `LoginScreen.test.tsx`, Jest + RNTL, 4 casos): sucesso navega com a mensagem certa e nunca autentica, erro exibe mensagem e não navega, sucesso de login aciona `login(email, password)`, banner de confirmação renderiza a partir do parâmetro de rota. **1 causa raiz de flakiness encontrada e corrigida, relevante para todo o resto do projeto:** os testes novos usavam `fireEvent.press`/`fireEvent.changeText` **sem `await`** — a mesma pegadinha da API assíncrona do `@testing-library/react-native` v14 já registrada no `AGENT_LOG.md` da Sprint 0 do frontend (13:00), mas não seguida desta vez. Sem o `await`, o segundo teste de cada arquivo falhava com a árvore renderizada vindo `null` (`toJSON()`) e o console acusando "overlapping act() calls" — isolado por repro mínimo (`render()` duas vezes no mesmo arquivo funciona normalmente; só quebra depois de um `fireEvent` de submit assíncrono não aguardado). Corrigido adicionando `await` em todos os `fireEvent.*` das duas suítes; também trocado o uso do `screen` global por dupla desestruturada do retorno de `render()` (mais robusto a esse tipo de contaminação entre testes). Verificação final completa: `tsc --noEmit`, `eslint .` e `jest --coverage` (26 testes, 8 suítes, todos verdes) sem erro; `expo-doctor` 18/18; `expo start` bundlou com sucesso (HTTP 200 no bundle). **Não feito nesta sessão, sinalizado explicitamente:** teste manual passo a passo contra o backend real (Sprint 1 do backend já está implementada e testada por outra sessão, mas não foi executada em conjunto com este ajuste nesta sessão) — recomenda-se validar o fluxo completo registro→banner de confirmação→login assim que ambos os serviços forem levantados juntos. `prompt-frontend-ux-fix.md` apagado conforme instrução do próprio arquivo. | `frontend/navigation/types.ts`, `frontend/screens/Auth/RegisterScreen.tsx`, `frontend/screens/Auth/LoginScreen.tsx`, `frontend/screens/Auth/RegisterScreen.test.tsx` (novo), `frontend/screens/Auth/LoginScreen.test.tsx` (novo), `AGENT_LOG.md` |
| 2026-07-09 10:15 | Multi | Correção | Diagnosticado ao vivo, com o backend rodando de verdade pela primeira vez (subido nesta sessão após corrigir falta de Maven/JDK 21 no PATH — ver entrada anterior — e pasta `backend/data/` ausente): o link de confirmação de e-mail (`irontrack://verify-email/{token}`) é logado corretamente (`SMTP_HOST` vazio funciona como projetado), mas é inabrível a partir do Expo Go (esquema de URL customizado só funciona em build standalone). Usuário decidiu (Tipo B, decisão explícita): desativar verificação de e-mail no fluxo ativo em vez de contornar o Expo Go — registrado como `ADR-018` em `13`. Patches aplicados: `00` §4.3, `03` §2.1/§2.2/§2.6 (registro auto-verifica, login não checa mais `email_verified_at`, `GET /auth/verify-email/{token}` marcado dormente — nada removido fisicamente, reversível sem migração), `04` (mensagem de `RegisterScreen` sem menção a e-mail, `VerifyEmailScreen` dormente), `07` §C.1 (nota sobre o novo comportamento). Também diagnosticado (não é bug): login caindo em `ProfileScreen` em vez de uma home — `AppStack` hoje só tem `ProfileScreen` cadastrada, porque `DashboardScreen` é escopo da Sprint 2 (ainda não feita). Usuário optou por uma `DashboardScreen` placeholder mínima agora (não a versão completa da Sprint 2) para poder apresentar o app. Criados `prompt-backend-disable-email-verification.md` e `prompt-frontend-simplify-and-home.md` (mensagem de registro + placeholder de Dashboard + `AppStack` atualizado, deixando explícito que a Sprint 2 vai expandir o mesmo arquivo, não criar um novo). | `00`, `03`, `04`, `07`, `13`, `AGENT_LOG.md` |
| 2026-07-09 15:00 | Backend | Correção | Executado `prompt-backend-disable-email-verification.md` (ADR-018, `13_ADR_LOG.md`) — mudança pequena e precisa, exatamente como especificado: `AuthServiceImpl.register()` agora seta `email_verified_at = now()` diretamente, sem gerar `email_verification_token_hash`/`email_verification_expires_at` (ficam `null`) nem chamar `EmailService.sendVerificationEmail` (constante `EMAIL_VERIFICATION_TTL`, órfã após a mudança, removida); `AuthServiceImpl.login()` teve a checagem `email_verified_at IS NULL → 403 EMAIL_NOT_VERIFIED` removida (comentário no lugar explicando o porquê, `errorCode` continua dormente no catálogo). `GET /auth/verify-email/{token}` **não** foi tocado — endpoint e método de serviço permanecem implementados e alcançáveis, só não são mais chamados a partir do registro. `forgotPassword`/`resetPassword` confirmados intactos. Testes atualizados para refletir o novo comportamento (não deletados, apenas os que validavam o bloqueio antigo): `AuthServiceTest` (teste de registro agora confirma `emailVerifiedAt` preenchido e `sendVerificationEmail` nunca chamado; teste de login com `EMAIL_NOT_VERIFIED` virou teste confirmando que o login funciona mesmo com `email_verified_at` nulo), `AuthControllerIT` (fluxo principal reescrito sem o passo de verificação intermediário, com asserção explícita de que o e-mail de verificação não é disparado), `UserControllerIT` (helper `registrarEVerificar` simplificado para `registrar`, sem captura/uso de token). Verificação final completa: `mvn clean install` — 85 testes unitários + 9 de integração verdes, gate JaCoCo em 80% mantido. Teste manual via `curl`: registro retorna `emailVerifiedAt` já preenchido, login funciona imediatamente sem nenhum passo intermediário, `forgot-password`/`reset-password` seguem funcionando normalmente ponta a ponta, `GET /auth/verify-email/{token}` continua respondendo `400 INVALID_OR_EXPIRED_TOKEN` (não `404`) para token inválido, confirmando que o endpoint permanece vivo e alcançável. `prompt-backend-disable-email-verification.md` apagado conforme instrução do próprio arquivo. | `backend/.../services/impl/AuthServiceImpl.java`, `backend/.../test/.../AuthServiceTest.java`, `backend/.../test/.../controllers/AuthControllerIT.java`, `backend/.../test/.../controllers/UserControllerIT.java`, `AGENT_LOG.md` |
| 2026-07-09 10:45 | Frontend | Correção | Executado `prompt-frontend-simplify-and-home.md`. **Parte A:** `RegisterScreen` — mensagem de confirmação pós-cadastro trocada de "Conta criada! Verifique seu e-mail e faça login." para "Conta criada! Faça login." (`13_ADR_LOG.md` ADR-018), resto do fluxo inalterado (continua navegando para `LoginScreen`, continua nunca autenticando automaticamente); `RegisterScreen.test.tsx` ajustado para o novo texto. `VerifyEmailScreen` não tocada (fica dormente, já registrada na navegação, sem nenhum lugar novo a invocá-la). **Parte B:** `screens/Dashboard/DashboardScreen.tsx` criado — placeholder mínimo pré-Sprint 2 (comentário explícito no topo apontando `08_ROADMAP_FRONTEND.md` §C.2 tarefa 4 como o que vai expandir este mesmo arquivo, não substituí-lo por um novo), exibindo "Bem-vindo(a), {name}!" via `useAuth()` e um botão "Ver perfil" navegando para `Profile`. `navigation/types.ts` (`AppStackParamList`) e `navigation/AppStack.tsx` atualizados: `Dashboard` agora é a primeira rota do stack, `Profile` alcançável a partir dela — login não cai mais direto em `ProfileScreen`. `DashboardScreen.test.tsx` criado (saudação com nome do usuário mockado via `useAuth`, e navegação para `Profile` ao tocar o botão). Verificação final completa: `tsc --noEmit`, `eslint .` (0 erros, `--fix` aplicado a 1 warning de formatação) e `jest --coverage` (27 testes, 9 suítes, todos verdes) sem erro; `expo-doctor` 18/18; `expo start` bundlou com sucesso (HTTP 200 no bundle). **Não feito nesta sessão, sinalizado explicitamente:** teste manual passo a passo (registrar → mensagem sem menção a e-mail → logar → cair em `DashboardScreen` → navegar para `ProfileScreen`) não foi executado de ponta a ponta contra o backend real nesta sessão — recomenda-se validar assim que `prompt-backend-disable-email-verification.md` estiver concluído e os dois serviços rodando juntos. `prompt-frontend-simplify-and-home.md` apagado conforme instrução do próprio arquivo. | `frontend/screens/Auth/RegisterScreen.tsx`, `frontend/screens/Auth/RegisterScreen.test.tsx`, `frontend/screens/Dashboard/DashboardScreen.tsx` (novo), `frontend/screens/Dashboard/DashboardScreen.test.tsx` (novo), `frontend/navigation/types.ts`, `frontend/navigation/AppStack.tsx`, `AGENT_LOG.md` |
| 2026-07-09 17:30 | Frontend | Correção | Executado `prompt-frontend-design-system.md` — retrofit visual completo de `docs/15_DESIGN_SYSTEM_UI_UX.md`/ADR-019 (dark-only, acento único vermelho neon, Oswald+Inter, MaterialCommunityIcons) sobre todo o código já existente das Sprints 0/1, sem nenhuma tela nova de Sprint 2+. **Dependências** (`15` §J): `@expo-google-fonts/oswald`, `@expo-google-fonts/inter`, `expo-font`, `expo-haptics`, `@expo/vector-icons` — mais `expo-asset` e o plugin babel `react-native-reanimated/plugin` (nenhum dos dois no prompt, mas ambos pré-requisitos reais descobertos na hora: `expo-font`/`@expo/vector-icons` não resolviam `expo-asset` sem estar hoisted na raiz — `Cannot find module 'expo-asset'` — e `withTiming`/callbacks do Reanimated exigem o plugin babel para não quebrar em runtime; `expo-doctor` ficou 18/18 depois de ambos). **Tokens** (`theme/colors.ts`/`typography.ts`/`spacing.ts`/`motion.ts`) espelhando `15` §B/§C/§E/§G.1; `tailwind.config.js` estendido com cores (`bg`, `accent`, `accent-dim`, etc.), `fontFamily` (Oswald/Inter), `borderRadius` e `fontSize` customizado (`display-xl/lg/md`, `body-lg/md/sm`, `numeric`) para bater exatamente com os px/line-height/letter-spacing de `15` §C.1 em vez de aproximar pela escala padrão do Tailwind. **`App.tsx`**: `useFonts` (Oswald 500/700, Inter 400/500) segurando a splash (`expo-splash-screen`) até `fontsLoaded`; `ToastProvider` montado acima do `AuthProvider`/`RootNavigator`; `StatusBar` trocado para `style="light"` (app é dark-only). **Componentes-base retrofitados** (`15` §H.1-H.4, API pública preservada): `Button` — variantes `primary`/`secondary`/`destructive`/`icon` (renomeado de `critical`→`destructive` e prop booleana `critical`→`large`, nenhum outro call site usava a prop booleana), glow via `shadow*`/`elevation` nas variantes primary/destructive, *scale* de toque via Reanimated, haptics `impactAsync(Light)` embutido na variante primary; `Input` — estilo underline, ícone de prefixo opcional, cor de foco/erro; `Card` — hairline sem sombra; `Modal` — `animationType="fade"` (confirmação simples, centralizada) vs. `"slide"` (destrutiva, sheet ancorado à base — reaproveita a prop já existente em vez de criar uma nova). **Novos componentes** (`15` §H.5): `Toast`+`ToastContext`/`useToast` (fila de 1, swipe via `PanResponder` — sem lib de terceiros — auto-dismiss por variante, testado com `jest.useFakeTimers`); `Header` (seta de voltar condicional, título `type.display.lg`/`md`, no máx. 1 ícone de ação) — usado em **ambas** as stacks (Profile/Dashboard no AppStack, ForgotPassword/ResetPassword/VerifyEmail no AuthStack), não só no AppStack como a redação literal do `H.5` sugeria — a própria tabela `15` §I já pedia "Header padrão" para as três telas do AuthStack, então o componente foi projetado stack-agnostic desde o início; `Wordmark` (compartilhado entre Login `xl`/Register `lg`). **Navegação**: `AuthStack`/`AppStack`/`RootNavigator` com `headerShown:false` + `contentStyle`/tema escuro (`NavigationContainer theme`); `RegisterScreen` ganhou campo "Confirmar senha" (só client-side, nunca enviado à API) para bater com a tabela `15` §I, que já listava "senha/confirmação" — não existia na Sprint 1. **Telas retrofitadas** (`15` §I): `LoginScreen` (wordmark, ícones `email`/`lock`, erros/sucesso via toast, exceção deliberada: `ACCOUNT_PENDING_DELETION` continua inline por precisar ficar ancorado ao botão "Cancelar exclusão"); `RegisterScreen` (wordmark menor + seta de volta, toast de erro); `ForgotPasswordScreen`/`ResetPasswordScreen`/`VerifyEmailScreen` (Header padrão, ícones `lock-reset`/`email-check`/`alert-circle`); `ProfileScreen` (Header voltando para Dashboard, seção "Excluir conta" isolada com `mt-xl`, `Modal` destrutivo em modo sheet, haptics `impactAsync(Medium)` ao abrir o diálogo); `DashboardScreen` (Header com saudação `Olá, {name}` + ícone de perfil no canto superior direito, **substituindo** o botão solto "Ver perfil" da sessão anterior). Haptics (`15` §F.4) cobertos em todos os 5 pontos listados no prompt. `app.json`: `userInterfaceStyle`→`dark` e `backgroundColor` da splash `#ffffff`→`#0A0A0C` (Tipo A não pedido explicitamente, mas necessário — sem isso um flash branco apareceria na abertura, contradizendo um app dark-only; não mexido no ícone/adaptive icon, que o próprio prompt marcou fora de escopo). **Fora de escopo, conforme instruído — não implementado:** `components/layout/TabBar.tsx` (sem consumidor até `AppStack` virar `BottomTabNavigator` na Sprint 2), nenhuma tela de Sprint 2+, ícone real do app/assets. **Simplificações deliberadas, sinalizadas:** *shake* de erro em validação inline (`15` §F.3) não implementado (só borda/ícone/texto — o motion de shake ficou de fora por não ter sido testado com tempo hábil nesta sessão); glow neon é aproximado via `shadow*`/`elevation` (sem lib nativa extra), fidelidade visual exata fica para o QA manual do usuário, como o próprio prompt já previa. Verificação final completa: `tsc --noEmit`, `eslint .` (0 erros, `--fix` aplicado a warnings de formatação) e `jest` (30 testes, 10 suítes, todos verdes, incluindo 2 arquivos de teste inteiramente novos — `Toast`/`ToastContext` e ajustes em `RegisterScreen`/`LoginScreen`/`DashboardScreen`) sem erro; `expo-doctor` 18/18; `expo start` bundlou 1595 módulos Android com sucesso. **Não feito nesta sessão:** teste manual em dispositivo físico (o próprio prompt já reserva isso ao usuário). `prompt-frontend-design-system.md` apagado conforme instrução do próprio arquivo. | `frontend/theme/` (novo), `frontend/tailwind.config.js`, `frontend/babel.config.js`, `frontend/App.tsx`, `frontend/app.json`, `frontend/components/common/{Button,Input,Card,Modal,Toast,Wordmark}.tsx`, `frontend/components/layout/Header.tsx` (novo), `frontend/contexts/ToastContext.tsx` (novo), `frontend/hooks/useToast.ts` (novo), `frontend/navigation/{AuthStack,AppStack,RootNavigator}.tsx`, `frontend/screens/Auth/*`, `frontend/screens/Dashboard/DashboardScreen.tsx`, `AGENT_LOG.md` |
| 2026-07-09 17:45 | Frontend | Auditoria | Usuário pediu para continuar `prompt-frontend-design-system.md` a partir da §4.7 (Login/Register/Forgot/Reset/VerifyEmail/Dashboard) — ao investigar, o arquivo já não existia e a entrada anterior (17:30) mostrou que outra sessão já tinha executado a tarefa inteira, `screens` incluídas, e já apagado o prompt. Confirmado por leitura direta de cada arquivo (não só do log) que as 6 telas already batem com `docs/15` §I. Única lacuna real: `LoginScreen.tsx` ainda tinha uma função `Wordmark` local duplicada em vez de importar o componente compartilhado (`RegisterScreen` já importava de `components/common/Wordmark`, mas o arquivo em si não existia) — criado `components/common/Wordmark.tsx` (`size: 'xl' | 'lg'`) e `LoginScreen` migrado para usá-lo. Reverificação completa: `tsc --noEmit`, `eslint .`, `jest --coverage` (30 testes, 10 suítes) e `npx expo export --platform android` (1469 módulos) sem erro; `expo-doctor` 18/18. | `frontend/components/common/Wordmark.tsx` (novo), `frontend/screens/Auth/LoginScreen.tsx`, `AGENT_LOG.md` |
| 2026-07-09 18:20 | Frontend | Correção | Usuário testou em dispositivo físico e reportou conteúdo sobreposto pela status bar/câmera (topo) e pela barra de gestos/home indicator (base) — nenhuma tela usava `SafeAreaView` de `react-native-safe-area-context` (dependência já presente, mas não consumida); `App.tsx` só monta `SafeAreaProvider` na raiz, o que sozinho não aplica inset a nada, e todo `headerShown` do React Navigation está desligado (`AuthStack`/`AppStack`/`RootNavigator`), então nenhum inset automático existia em lugar nenhum. Criado `components/layout/Screen.tsx` (wrapper único, `SafeAreaView` com `edges` configuráveis, default `['top','bottom','left','right']`) e aplicado como raiz de `LoginScreen`, `RegisterScreen`, `ForgotPasswordScreen`, `ResetPasswordScreen`, `VerifyEmailScreen` (2 pontos de retorno — sucesso e formulário), `ProfileScreen`, `DashboardScreen` e do estado de loading em `RootNavigator` — substituindo o `<View className="flex-1 bg-bg">`/`<ScrollView className="flex-1 bg-bg">` solto que cada uma usava como raiz. Nenhuma mudança de lógica/API de componente, correção puramente de layout. Verificação final completa: `tsc --noEmit`, `eslint . --fix` (0 erros, reindentação automática) e `jest` (30 testes, 10 suítes, todos verdes) sem erro; `expo-doctor` 18/18. Teste manual em dispositivo físico fica com o usuário (já confirmado como o próximo passo dele). | `frontend/components/layout/Screen.tsx` (novo), `frontend/screens/Auth/{LoginScreen,RegisterScreen,ForgotPasswordScreen,ResetPasswordScreen,VerifyEmailScreen,ProfileScreen}.tsx`, `frontend/screens/Dashboard/DashboardScreen.tsx`, `frontend/navigation/RootNavigator.tsx`, `AGENT_LOG.md` |
