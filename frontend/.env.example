# Copie este arquivo para `.env` (nunca commitado) e ajuste os valores locais.
# EXPO_PUBLIC_* é inlined no bundle JS em build-time pelo Metro (05_DEVOPS_E_SEGURANCA.md §D).
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1
